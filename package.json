{
  "name": "dirt-backend",
  "version": "1.0.0",
  "description": "Backend WebSocket server for the DIRT Unraid plugin.",
  "main": "dirt.js",
  "scripts": {
    "start": "node nodejs/dirt.js >> dirt_server.log 2>&1 &",
    "stop": "ps aux | grep 'dirt.js' | grep -v grep | awk '{print $2}' | xargs kill -9",
    "restart": "npm run stop && npm run start",
    "seed": "node tests/helpers/seed-redis.helper.js",
    "unraid-workaround": "tail -n +5 dirt-tables.page > index.php",
    "clean": "rm -rf dirt_server.log index.php jules-scratch",
    "prep": "npm install && playwright install && npm run seed",
    "test": "npm run clean && npm run unraid-workaround && npm run start && pytest && npm run stop"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "blake3": "^3.0.0",
    "bullmq": "^5.7.8",
    "luxon": "^3.5.0",
    "redis": "^4.6.13",
    "redis-om": "^0.4.7",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@playwright/test": "^1.44.1"
  },
  "overrides": {
    "blake3-wasm": "3.0.0",
    "@c4312/blake3-internal": "3.0.0"
  }
}
